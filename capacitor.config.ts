import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.app",
  appName: "capacitor-nextjs-sample",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
